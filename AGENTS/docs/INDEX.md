# semaphore-tests — documentation

E2E test suite for Semaphore UI. Playwright + pytest, SQLite, real server from source.

## Rules

- Create a file in `AGENTS/docs/` for reference information that needs to be cited repeatedly.
- Update it when the described subject changes — stale descriptions are worse than no documentation.

## Inventory

No docs yet. Reference information lives in `CLAUDE.md` and `README.md` for now.
